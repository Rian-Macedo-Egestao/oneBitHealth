import { StyleSheet } from "react-native";

const style = StyleSheet.create(
    {
        boxTitle: {
            alignItems: "center",
            alignContent: "center",
            padding: 30,
        },
        textTitle: {
            color: "#ff0043",
            fontSize: 32,
            fontWeight: "bold",
        }, 
    }
);

export default style;